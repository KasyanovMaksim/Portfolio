import { NoiseBackground } from './noise-background';

describe('NoiseBackground', () => {
  it('should create an instance', () => {
    const directive = new NoiseBackground();
    expect(directive).toBeTruthy();
  });
});
